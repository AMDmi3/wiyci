[build]
# * Enable unstable tokio features, namely metrics
# * Enable unstable sqlx features, namely proper recompilation on migrations/ directory changes
rustflags = ["--cfg=tokio_unstable", "--cfg=sqlx_macros_unstable"]

[env]
# Since most tests use the database, limit concurrency not to cause excess load on it
RUST_TEST_THREADS = "4"
